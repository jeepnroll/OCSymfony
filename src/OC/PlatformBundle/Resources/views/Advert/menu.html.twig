<ul class="list-group">
    {% for advert in listAdverts %}
    <li class="list-group-item list-group-item-action justify-content-between">
        <a class="" href="{{ path('oc_platform_view', {'id': advert.id }) }}">
            {{ advert.title}}
        </a>
        <span class="badge badge-default badge-pill">New</span>
    </li>
    {% endfor %}
</ul>